<!DOCTYPE html>
<head>
  <meta charset="utf-8">
  <title>Shop</title>
</head>
<body>
  <%= link_to "Create New Item", new_store_item_path(@store), class: "badge badge-success center" %>
  <div class="container">
    <div class="row">
      <% @items.each do |item| %>
        <div class="center-block">
          <div class="card border-white" style="width: 15rem;">
            <%= image_tag item.image.url, size: "250" %>
            <div class="card-body">
              <center><p><strong><%= link_to item.title, store_item_path(item.store, item) %></strong></p>
                <!-- <p>Description: <%= item.description %></p> -->
                <p>Price: <%= number_to_currency(item.price) %></p>
                <% unless item.retired? %>
                  <%= link_to "Add to cart", carts_path(item_id: item.id), method: :post, class: "badge badge-primary" %></center>
            <% else %>
              <div class="badge badge-secondary">
                Retired
              </div>
            <% end %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>

</body>
